function runAcheivements (data) {
    console.log(data) ;
    // if (data.comments.indexOf('lick'))
    console.log("Acheivements running...");
    console.log(document);
    // offensive/defensive: "Offensive"
    // overUnder: "Over"
    var highscore = 0;
    
    
    if (comment.indexOf("rainbow") != -1){
        $('.content').css('background','linear-gradient(to bottom, rgba(255,44,40,1')
    }

    if (data.comments.indexOf('lick') !==-1) {
        alert("Don't Lick the Checkbox.");
    }
    if (data.comments.indexOf('lick') !==-1) {
        alert("Don't Lick the Checkbox.");
    } if (data.comments.indexOf('flipped over') !==-1) {
        alert('Robot Tippin\' Season');
    } if (data.comments.indexOf('first') !==-1) {
        alert('FIRST: Freaking Imaginative Robots Still Trash');
    } if(data.comments.indexOf('Doctor') !==-1) {
        alert('I\'m a robot, not a doctor');
    } if(data.comments.indexOf('water') !==-1) {
    alert('Not a Water Game. \n Type water');
    } if(data.comments.indexOf('bollyball') !==-1) {
        alert('Is is volleyball or bollyball?');
    } if(data.comments.indexOf('password') !==-1) {
        alert('Hey Ryan');
        $("#textarea").css("-webkit-text-security", "disc");                            
    } if (data.comments.indexOf(';') !==-1) {
        alert('Why would you use a greek question mark?')
    } if (data.comments.indexOf('rick') !==-1) {
        var window = window.open("https://www.youtube.com/watch?v=dQw4w9WgXcQ");
        
    }
     // if (data.comments.indexOf(''))

//

}
export default runAcheivements;